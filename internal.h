/*
   	The command interpreting function . 
	In the small language , all lines map to a definitive internal function ,
	so I must suply enough functions here .

	Here are all the so called "standard operation" functions , they based on
	the really built data structure , so they are just algorithms .

									*/

#ifndef _INTERNAL_H

#define _INTERNAL_H

int def(void);
int etn(void);
int prt(void);
int had(void);
int tal(void);
int dpt(void);
int cnt(void);
int inv(void);
int ni(void);

#endif
